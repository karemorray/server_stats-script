# server_stats-script
Presenting simple script gives the server_stats when run.
Stats like Health check of server on parameters like CPU,Mem,Swap,Disk_usage,Failed services,Network,Kernel,OOM events,Load average etc.
Intentionally avoided the complex logic.Tweak this script as per need/use case. 
